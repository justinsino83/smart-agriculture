<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.weiming.smartag.mapper.SoilDataMapper">
    
    <select id="selectLatestBySensorId" resultType="com.weiming.smartag.entity.SoilData">
        SELECT * FROM soil_data WHERE sensor_id = #{sensorId} ORDER BY collect_time DESC LIMIT 1
    </select>

    <select id="selectHistoryData" resultType="com.weiming.smartag.entity.SoilData">
        SELECT * FROM soil_data WHERE sensor_id = #{sensorId} AND collect_time BETWEEN #{start} AND #{end} ORDER BY collect_time ASC
    </select>
    
</mapper>
